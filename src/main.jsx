import React from 'react'
import ReactDOM from 'react-dom/client'
import ComprehensiveChannelAnalysis from './channel_analysis.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ComprehensiveChannelAnalysis />
  </React.StrictMode>,
)
